미니 프로젝트
-서버를 분리시키는 것을 추천

3Tier
Presentation (눈에 보이는 부분) : 자바 스크립트 알아야 함(프론트엔드)
Application (실제 동작, 기능) : Flask 등 (백엔드)
Data (데이터 처리, 입력/수정/조회) : MariaDB, MySQL 등 (DB)

어플리케이션과 데이터를 하나로 묶고, 프레젠테이션까지 해서 2 Tier로 가도 된..다?

■ HTTP Method
● FORM 데이터 활용 (POST)
-회원가입/댓글쓰기/파일업로드 같은 기존에 없던 데이터에서 창조하는 행위들을 할 때 쓰는 방식
-DB에서의 CRUD에서 R(read)를 제외한 모든 부분이 post 방식?

● FORM 데이터 활용 (GET)
-조회할 때 주로 사용하고 대배분의 FORM방식?80~90%쓰인다, 특별한 작업을 하지 않는 이상 get 방식

Flask
웹페이지에서 vscode에서 입력한 값을 찾지 못하면 웹페이지에서 &를 계속 붙여가며 값을 추가 할 수 있다.(하지만 사용자 입장에서는 번거로움)
ex)https://127.0.0.1:5000/join?age=123&name=abc 

그래서 처음 /join 접속시 입력화면 제공(get)
데이터 입력 후 버튼 클릭

html의 주석처리 ctl + /